int calculateArea(int width, int height) {
    int area = 0, perimeter = 0;

    area = width * height;
    return area;
}

void main() {
    int w, h, result;
    w = 10;
    h = 5;

    result = calculateArea(w, h);
}