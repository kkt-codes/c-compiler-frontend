int calculateArea(int width, int height) {
    int area, perimeter;

    area = width * height;
    return area;
}

void main() {
    int w, h, result;
    w = 10;
    h = 5;

    result = calculateArea(w, h);
}